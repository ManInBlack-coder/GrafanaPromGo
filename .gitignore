# Ignore the entire extracted Prometheus server folder
prometheus-server/prometheus-2.45.0.darwin-arm64/

# Also ignore the Prometheus data directory, in case it's created elsewhere
# (though it should be inside the above ignored folder by default)
prometheus-server/data/

# Ignore downloaded archives
*.tar.gz
*.zip
*.dmg

# macOS specific files
.DS_Store

# Node.js related files (if applicable to your Grafana/Go project)
node_modules/
npm-debug.log
.env